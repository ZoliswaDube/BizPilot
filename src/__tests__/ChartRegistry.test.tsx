import { ChartRegistry } from '../components/charts/ChartRegistry';

describe('ChartRegistry', () => {
  it('should be defined', () => {
    expect(ChartRegistry).toBeDefined();
    // TODO: Add more specific tests for chart registry logic
  });
}); 